# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual Environment
venv/
env/
.env
.venv/
*.venv
poetry.lock

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Data (raw data should be downloaded, not committed)
data/raw/*.csv
data/raw/*.zip
data/processed/*.csv
data/processed/*.pkl

# Dataset files in project root (use data/raw/ instead)
ai4i2020.csv
ai4i+2020+predictive+maintenance+dataset.zip
*.zip

# Model artifacts (too large for git)
models/*.joblib
models/*.pkl
models/*.json

# Reports (allow figures and reports to be tracked)
# reports/figures/*.png
# reports/figures/*.svg
reports/results/*.json
reports/results/*.csv

# Streamlit — the theme config is part of the project; secrets are not
.streamlit/secrets.toml

# Temp files
*.tmp
*.bak
~$*

# Logs
*.log

# Local working notes & agent instructions (not part of the deliverable)
md/

# Assignment / submission documents (do not commit Word/PDF docs)
*.docx
*.pdf
ps_doc.docx

# Compiled binaries / build outputs
bin/
*.class

# Unrelated Java coursework kept out of the ML repo
ServletDemo/
git-final-project/

# Test coverage & type checking caches
.coverage
htmlcov/
.mypy_cache/
.pytest_cache/
